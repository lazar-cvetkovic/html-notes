// ---------------------------------------------------------------------------
// Fill these in with your Supabase project values (see SETUP.md).
// These are PUBLIC values and safe to commit:
//   - the anon key is designed for client-side use; writes are protected by
//     Row Level Security + Storage policies, not by hiding this key.
// ---------------------------------------------------------------------------
window.SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";

// The only account allowed to upload / delete notes. Must match the email of
// the Supabase user you created (and the email used in the SQL policies).
window.OWNER_EMAIL = "laki.cvetkovic@gmail.com";
